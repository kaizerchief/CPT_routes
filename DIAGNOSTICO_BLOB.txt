#!/usr/bin/env python
"""
Guía de diagnóstico y configuración de Vercel Blob
"""
print("""
═══════════════════════════════════════════════════════════════
  GUÍA DE CONFIGURACIÓN Y DIAGNÓSTICO - VERCEL BLOB
═══════════════════════════════════════════════════════════════

1️⃣  VARIABLES DE ENTORNO REQUERIDAS EN VERCEL
────────────────────────────────────────────────────────────────
   La única variable que necesitas es:
   • BLOB_READ_WRITE_TOKEN  (obtén del dashboard de Vercel)

   NO necesitas:
   • VERCEL_BLOB_URL (la genera vercel-blob automáticamente)
   • VERCEL_BLOB_BASE_URL (innecesaria)

2️⃣  INSTALANDO DEPENDENCIAS
────────────────────────────────────────────────────────────────
   Las siguientes ya están listadas en requirements.txt:
   • vercel-blob
   • requests
   • Flask
   • reportlab

3️⃣  PROBANDO LOCALMENTE SIN BLOB
────────────────────────────────────────────────────────────────
   Para ASEGURAR que funciona, primero prueba SIN Blob:
   
   a) NO defines BLOB_READ_WRITE_TOKEN
   b) Ejecuta: python test_flow.py
   c) Si funciona, entonces Blob NO es el problema
   d) Ejecuta la app: python -m flask run

4️⃣  DEBUGGING - SI NADA FUNCIONA
────────────────────────────────────────────────────────────────
   Revisa los logs agregando prints en app.py:
   
   En get_routes_state(), después de list_blobs():
      print(f"DEBUG: BLOB_TOKEN={bool(BLOB_TOKEN)}")
      print(f"DEBUG: blobs_response={blobs_response}")
      print(f"DEBUG: list returned {len(blobs_response.get('blobs', []))} items")
   
   En save_routes_state(), después de put():
      print(f"DEBUG: put() completó para {VERCEL_BLOB_OBJECT_NAME}")

5️⃣  PASOS RECOMENDADOS DE DIAGNÓSTICO
────────────────────────────────────────────────────────────────
   ✓ Paso 1: Ejecuta test_flow.py (ya está hecho, funcionó)
   ✓ Paso 2: Limpia archivos temporales locales
            rmdir c:\\Users\\lenovo\\AppData\\Local\\Temp\\cpt_uploads /s /q
   ✓ Paso 3: Ejecuta la app con: python -m flask run
   ✓ Paso 4: En http://localhost:5000, prueba:
            - Carga un CSV
            - Ve si aparece en "Ver Rutas"
            - Si funciona, tu Blob está OK
   ✓ Paso 5: Si no funciona, habilita DEBUG en get_routes_state()

6️⃣  EXPLICACIÓN DEL CÓDIGO
────────────────────────────────────────────────────────────────
   save_routes_state():
   • Si BLOB_TOKEN → guarda en Vercel Blob con put()
   • Si no → guarda en archivo local /Temp/cpt_uploads/

   get_routes_state():
   • Si BLOB_TOKEN → busca en Blob con list_blobs() y GET el blob_url
   • Si no → busca el archivo JSON local
   • En ambos casos, valida que no haya expirado (10 horas)

═══════════════════════════════════════════════════════════════
""")
